# Build output is generated by `vite build` and embedded at compile time via
# go:embed. Keep the committed placeholder index.html (so the Go package always
# compiles) and this file; ignore everything else the build emits.
*
!.gitignore
!index.html
